# Optional: one-time seed for API URL when device storage is empty.
# Without this (production multi-client APK), the app asks for the CRM URL on first launch.
EXPO_PUBLIC_API_URL=http://localhost:3001
