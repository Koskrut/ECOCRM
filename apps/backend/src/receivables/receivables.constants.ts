/** Tolerance for ALIGNED status (base currency units). */
export const RECEIVABLES_DELTA_TOLERANCE = 0.01;

/** 1C Excel amounts are treated as UAH unless a currency column is added later. */
export const RECEIVABLES_1C_AMOUNT_CURRENCY = "UAH";
