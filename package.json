{
  "name": "crm",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "lint": "eslint . -c eslint.config.mjs",
    "lint:fix": "eslint . -c eslint.config.mjs --fix",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:web": "npm run dev --workspace=web",
    "dev:store": "npm run dev --workspace=store",
    "dev:gateway": "npm run dev --workspace=gateway-service",
    "import:characteristics": "npm run import:characteristics --workspace=backend --",
    "import:characteristic": "npm run import:characteristic --workspace=backend --"
  },
  "devDependencies": {
    "eslint-plugin-prettier": "^5.5.5"
  }
}
