"use strict";

/**
 * Canonical timeline runtime constants shared between backend and web.
 * Type definitions live in ./timeline.d.ts.
 */
module.exports = {
  TIMELINE_PAGE_SIZE: 60,
  TIMELINE_MAX_LIMIT: 200,
};
