Пиши только TypeScript.

Не добавляй новые зависимости без явного запроса.

Следуй требованиям из docs/PRD.md, docs/DOMAIN.md, docs/UX.md.

Backend: вся бизнес-логика и интеграции только на backend.

Любое изменение статуса заказа — только через единый сервис статусов и запись в историю.

Любая интеграция (Nova Poshta, Privat24) — только через apps/backend/src/integrations/* + интерфейс + мок для тестов.

Валидация входных данных обязательна (DTO/валидаторы).

Перед завершением задачи: код должен проходить lint/typecheck/test.

Пиши маленькими коммитоподобными шагами: план → шаг 1 → шаг 2.